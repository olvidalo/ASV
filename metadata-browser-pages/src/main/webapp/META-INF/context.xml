<?xml version="1.0" encoding="UTF-8"?>
<Context antiJARLocking="true" path="/metadata-browser-pages">
  <Parameter description="Default Spring profile, may be overridden using spring.profiles.active" name="spring.profiles.default" value="hybrid-production"/>
  <!-- 
	Spring profile for runtime. Actual beans may come from dependency, also see contextConfigLocation parameter in web.xml. 
	

	Profiles:
	    * hybrid-production: combines all hybrid production profiles
	     * hybrid-production-providers: actual providers (for node presentation, icons, etc) implementation
	     * hybrid-production-csdb: explicitly select adapter implementation of corpus structure provider that connects to the 'old' (IMDI) corpus structure API
	     * hybrid-production-ams: explicitly select adapter implementation of License and Authorization services that wrap 'old' AMS services

	    * cmdi-mock-csdb: mock implementation of CMDI CSDB
	    * cmdi-mock-ams: mock implementation of AMS License and Authorization services
    -->
</Context>
